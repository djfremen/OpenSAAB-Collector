# OpenSAAB branding

The PNG is the project's official OpenSAAB T2 logo, supplied by the project owner.
The ICO is a multi-resolution conversion of that PNG for the Windows executable.
Both are embedded in the Collector by build.ps1; no network fetch is used at launch.
